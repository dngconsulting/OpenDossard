name: docker-compose-actions-workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build Open Dossard images 
        run: docker-compose -f docker-compose.prod.yml build --no-cache --force-rm --parallel 
